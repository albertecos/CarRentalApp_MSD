# CarRentalApp_MSD

## How to run

### CarRental (Frontend)

From the CarRental folder run the command
```bash
npx expo start
# or for ease of use
npx expo start --web
```

### Server
From the Server folder run the command

```bash
node server.js
```